# PostgreSQL (Docker)
POSTGRES_USER=cinescope
POSTGRES_PASSWORD=cinescope_dev
POSTGRES_DB=cinescope

# Express Server
DATABASE_URL=postgresql://cinescope:cinescope_dev@localhost:5432/cinescope
JWT_SECRET=change-me-in-production-use-a-long-random-string
PORT=4000

# TMDB API (optional — for NZB import auto-lookup)
TMDB_API_KEY=

# SABnzbd Downloader
SABNZBD_URL=http://192.168.1.100:8080
SABNZBD_API_KEY=your-sabnzbd-api-key
SABNZBD_CATEGORY=movies

# OpenMedia NZB Storage API
NZB_API_URL=http://localhost:4100

# Hetzner Object Storage (S3-compatible)
S3_ACCESS_KEY=your-access-key
S3_SECRET_KEY=your-secret-key
S3_ENDPOINT=https://hel1.your-objectstorage.com
S3_BUCKET=openmedia-files
S3_REGION=hel1

# Hetzner Cloud API (for VM management)
HETZNER_API_TOKEN=your-hetzner-cloud-api-token

# NZB Service (serves NZB files by hash)
NZB_SERVICE_URL=https://nzb.nettoken.de

# Download Provisioning: "hetzner" (production) | "local" (dev) | "false" (disabled)
AUTO_PROVISION=hetzner

# Service token for download container callbacks (required for hetzner mode)
SERVICE_API_TOKEN=

# Docker image for download containers
DOWNLOADER_DOCKER_IMAGE=ghcr.io/ichbinder/openmedia-downloader:latest

# Usenet servers (JSON array, used by provisioner to inject into download VPS)
# Example: [{"name":"eweka","host":"news.eweka.nl","port":563,"ssl":true,"username":"u","password":"p","connections":20}]
USENET_SERVERS=

# Hetzner Cloud private network ID (optional)
HETZNER_NETWORK_ID=
HETZNER_SSH_KEY_NAME=

# Encryption master key for at-rest credential encryption (64 hex chars / 256-bit)
# Generate with: openssl rand -hex 32
ENCRYPTION_MASTER_KEY=

# Public-facing API base URL (used by provisioner to give download containers a callback URL)
# Set to your own deployment URL — defaults to localhost for safety
API_BASE_URL=http://localhost:4000

# How long a needs_review download job stays around before automatic cleanup.
# When an extension uploads an NZB whose title cannot be matched to a TMDB movie,
# the job lands in needs_review and waits for the user to manually assign a film.
# After this many days without assignment the job is failed and the orphan NzbFile
# (plus its physical file on the NZB service) is removed.
REVIEW_RETENTION_DAYS=3

# HMAC-SHA256 secret for signed media streaming URLs (min 32 characters).
# Used by the Jellyfin plugin to generate short-lived signed URLs instead of
# embedding long-lived om_ tokens in STRM files.
# Generate with: openssl rand -base64 48
MEDIA_SIGNING_SECRET=
